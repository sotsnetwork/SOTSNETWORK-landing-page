<svg width="305" height="46" viewBox="0 0 305 46" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Cursor AI Logo -->
  <g>
    <!-- Cursor arrow shape -->
    <path d="M20 8L32 24L24 24L28 40L20 24L12 24L20 8Z" fill="hsl(var(--primary))"/>
    <!-- Cursor dot -->
    <circle cx="20" cy="24" r="2" fill="hsl(var(--foreground))"/>
    
    <!-- Cursor AI Text -->
    <text x="45" y="30" font-family="system-ui, -apple-system, sans-serif" font-size="18" font-weight="600" fill="hsl(var(--foreground))">
      Cursor AI
    </text>
  </g>
</svg>
